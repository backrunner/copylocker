/**
 * Published by the `@copylocker/unplugin` guard bootstrap (production webpack
 * build only): the actually-computed integrity-manifest root. `undefined` in
 * `next dev`, in Turbopack builds, and in builds without the plugin.
 */
// eslint-disable-next-line no-var
declare var __CL_GUARD_R__: Promise<Uint8Array> | undefined
