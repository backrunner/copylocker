# Build and package output
target/
dist/
build/
release/
*.node
**/gen/schemas/

# Local fuzz state; durable corpus storage is managed separately.
/fuzz/artifacts/
/fuzz/corpus/
/fuzz/crash-*
/fuzz/oom-*
/fuzz/timeout-*
/fuzz/leak-*

# JavaScript and Cloudflare local state
node_modules/
.next/
.wrangler/
.dev.vars
.dev.vars.*

# Local M4 build key material (dev signing keys, KEK registry, wrapping keys)
.copylocker/

# Local browser QA state and screenshots
/.playwright-cli/
/output/

# Local environment and OS files
.env
.env.*
!.env.example
.DS_Store
